import { FormatPublicUrlPipe } from './format-public-url.pipe';

describe('FormatUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPublicUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
